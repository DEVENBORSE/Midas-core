# Midas-core
Midas Core is a Spring Boot backend service that processes financial transactions using Kafka, validates them, stores data with JPA/H2, integrates an external Incentive API, and exposes user balances via REST endpoints.
